export default function RewardsPage() {
  return (
    <div>
      {/* TODO: /dashboard/rewards — 🎁 Reward一覧。ユーザー向けの閲覧・アンロック画面 */}
      <h2 className="text-on-surface">Reward一覧</h2>
    </div>
  );
}
