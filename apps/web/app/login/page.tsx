export default function LoginPage() {
  return (
    <div>
      {/* TODO: /login — GitHub OAuthログインボタン。loading/error stateはsearchParamsで判定する */}
      <h2 className="text-on-surface">ログイン</h2>
    </div>
  );
}
